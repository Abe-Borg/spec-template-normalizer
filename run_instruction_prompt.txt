Task:
Using the slim bundle, normalize CSI semantics by ensuring consistent paragraph styles for:
- Section header (SectionID optional + SectionTitle)
- PART headings
- Articles (1.01…)
- Paragraphs (A., B.)
- Subparagraphs (1., 2.)
- Sub-subparagraphs (a., b.)

You MUST also output a "roles" mapping (role → {styleId, exemplar_paragraph_index}) as described in the master prompt.

IMPORTANT: Classify all classifiable paragraphs (all non-skipped, non-table content paragraphs).
The apply_pStyle array must include every classifiable paragraph.
Skip only empty paragraphs, sectPr paragraphs, in-table paragraphs, "END OF SECTION", editor notes in brackets, copyright/distribution notices, and unbracketed specifier editing instructions.

Constraints:
- Do not change headers/footers or sectPr.
- Reuse existing paragraph styles from the style catalog when possible, even if those styles are currently unused.
- When numbering metadata exists, use it as structural evidence.
- If you create styles, do NOT describe formatting. Use derive_from_paragraph_index only.
- Return JSON instructions only (no prose, no XML, no markdown).
