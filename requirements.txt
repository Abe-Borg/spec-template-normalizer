# Direct runtime dependencies only (the modules the application imports).
# pip resolves the transitive closure under them, and
# packaging/windows/generate_third_party_notices.py inventories that closure
# from the real build environment, so nothing transitive is listed here.
# Test tools live in requirements-dev.txt; Windows build tools, including
# `packaging` (imported only by the notices generator), in
# requirements-build.txt.
anthropic==0.84.0
customtkinter==5.2.2
httpx==0.28.1
keyring==25.5.0
