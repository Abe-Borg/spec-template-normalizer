Task:
Give every input paragraph exactly one disposition: classify its CSI role using only
available_roles, or explicitly ignore it when it is genuine non-CSI content.

Output schema:
{
  "classifications": [
    { "paragraph_index": 12, "csi_role": "PART" }
  ],
  "ignored_paragraphs": [
    { "paragraph_index": 13, "reason": "non_csi_content" }
  ],
  "notes": []
}

IMPORTANT:
- The two disposition arrays together MUST cover every supplied paragraph exactly once
- Every heading, article, paragraph, and subparagraph with a CSI role must be classified
- Use ignored_paragraphs only for genuine non-CSI content, never for role uncertainty
- Every csi_role value MUST be one of the strings in available_roles
- Only skip truly empty/structural section-break paragraphs and "END OF SECTION" lines; classify visible paragraphs even when they contain w:sectPr
- Do not invent roles that aren't in available_roles
- When uncertain between two roles, prefer the more specific one (e.g., SUBPARAGRAPH over PARAGRAPH)
